# Base URL of the backend API.
# Local dev: leave blank — Vite proxies /api to http://localhost:4000.
# Production (Vercel): set to your Render URL, e.g. https://wc2026-api.onrender.com
VITE_API_URL=
